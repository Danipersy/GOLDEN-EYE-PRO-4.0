name: Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install numpy==2.2.6 numba pandas_ta streamlit pandas yfinance plotly
      - name: Test
        run: python -c "import pandas_ta; print('✅ OK')"
