cat > .github/workflows/deploy.yml << 'EOF'
name: Deploy

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - run: pip install pandas-ta
      - run: python -c "import pandas_ta; print('✅ Pronto')"
      - run: echo "🌐 Deploy su Streamlit Cloud - https://share.streamlit.io"
EOF
