name: Test Golden Eye Pro

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Installa dipendenze
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Verifica struttura file
        run: |
          echo "📁 Contenuto directory:"
          ls -la
          
          echo "\n📁 Verifica file essenziali:"
          if [ -f "App.py" ]; then echo "✅ App.py trovato"; else echo "❌ App.py mancante"; exit 1; fi
          if [ -f "config.py" ]; then echo "✅ config.py trovato"; else echo "❌ config.py mancante"; exit 1; fi
          if [ -f "requirements.txt" ]; then echo "✅ requirements.txt trovato"; else echo "❌ requirements.txt mancante"; exit 1; fi
      
      - name: Test import base
        run: |
          python -c "
import sys
print('🔍 Test importazioni base...')
try:
    import config
    print('✅ config OK')
except Exception as e:
    print(f'❌ config: {e}')
    sys.exit(1)
"
      
      - name: Test providers
        run: |
          python -c "
import sys
print('🔍 Test providers...')
try:
    from providers.base_provider import BaseProvider
    print('✅ base_provider OK')
except Exception as e:
    print(f'❌ base_provider: {e}')
    sys.exit(1)
"
      
      - name: Crea directory cache
        run: |
          mkdir -p cache
          mkdir -p cache/twelvedata
          mkdir -p cache/yahoo
          echo "✅ Cache directories create"
      
      - name: Summary
        run: |
          echo "🎉 Test completati con successo!"
          echo "📊 Stato: ✅ Pronto per Streamlit Cloud"
